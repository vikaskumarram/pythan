split() used  when converterd string into list 
syntex = 
text = "bananaa, apple"
fruits = taxt.split

join() used for converted list into string
syntex = 


dictionary() it is used when present different data type
syntex =


get(key)  is saf it is can provide a default value
it is ? (like optional chnainging)
